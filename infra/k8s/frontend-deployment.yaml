apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  labels:
    app: ai-bos
    component: frontend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: ai-bos
      component: frontend
  template:
    metadata:
      labels:
        app: ai-bos
        component: frontend
    spec:
      containers:
      - name: frontend
        image: ai-bos/frontend:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 80
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
        livenessProbe:
          httpGet:
            path: /health
            port: 80
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 80
          initialDelaySeconds: 10
          periodSeconds: 5
