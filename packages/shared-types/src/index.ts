// Shared TypeScript types between apps/web and apps/api
// Currently types are in apps/web/src/types/ (frontend-only usage).
// Move types here when they become truly shared between frontend and backend.

export * from './crm';
export * from './camera';
